import branches from './branches.json';

export const branchesData = branches.branchesData;
export const visibleStates = branches.visibleStates;
export const dropdownStates = branches.dropdownStates;
